# Obsidian Admin Laravel v1.3.0

A framework-forward release that upgrades the backend foundation to Laravel 13 and adopts the new framework capabilities that fit the current architecture without breaking the published API contract.

## Highlights

- Upgraded the backend baseline from Laravel 12 to Laravel 13
- Upgraded the test and tooling stack to match the Laravel 13 ecosystem:
  - Pest 4
  - PHPUnit 12
  - latest Laravel 13-compatible Octane / Pulse / Pennant / Scramble releases
- Adopted Laravel 13 request forgery protection via `PreventRequestForgery`
- Centralized audit queue routing with `Queue::route(...)`
- Adopted controller `#[Middleware]` attributes for high-value system endpoints
- Extended production OpenAPI caching with `Cache::touch(...)`
- Started adopting Laravel 13 Eloquent attributes in the live domain model:
  - `#[Boot]` on `User`, `Role`, and `AuditPolicy`
  - `#[UsePolicy]` on policy-backed access, tenant, and audit models
  - `#[Scope]` on `Role`, `User`, and `AuditLog` for stable repeated query semantics

## Laravel 13 feature adoption

This release does more than move the framework constraint.

The backend now uses Laravel 13 features in places where they improve clarity without changing the published API contract:

- model lifecycle hooks now use `#[Boot]` where tenant-scope normalization belongs on the model itself
- policy bindings now live on the models via `#[UsePolicy]`, which reduces provider noise and keeps authorization ownership closer to the model
- repeated role, user, and audit-log query semantics now use `#[Scope]`, which makes list and guard queries easier to read and safer to evolve

This keeps the repository aligned with current Laravel idioms while preserving the existing contract-driven architecture.

## Why this release matters

This release keeps the project on the current Laravel major while preserving the repository's existing strengths:

- typed service and controller boundaries
- tenant-safe behavior
- OpenAPI-first contract discipline
- official Octane / Horizon / Pulse / Reverb runtime support
- release-grade CI, supply-chain, and runtime verification

The goal was not to force every new Laravel 13 API into the project, but to adopt the ones that improve the current architecture without distorting the product model or the public contract.

## Verification

```bash
vendor/bin/pint --test
composer run quality:check
php artisan test
pnpm docs:build
composer run docs:path-safety
```

## Release Pair

- Backend: `v1.3.0`
- Compatible frontend release: `v1.2.0`
- Coordination note: this is a backend-only minor release because no published frontend contract break was required

## Notes

- This release upgrades the framework baseline, but it does not intentionally change the published API wrapper shape.
- Frontend `v1.2.0` remains the intended compatible release unless a later frontend patch is cut for documentation-only alignment.
- Frontend consumers should still follow the compatibility matrix before mixing tags.
